// Stub EA data fetcher for dev-ea command
export async function fetchEAData(discordUserId) {
    // TODO: Replace with real EA API logic
    return [
        { name: "Test League", teams: [{}, {}, {}] },
        { name: "Another League", teams: [{}] }
    ];
}
